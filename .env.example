# Public frontend origin(s) allowed to call /api/create-checkout-session.
# Comma-separated list is supported.
FRONTEND_ORIGIN=https://www.example.com
# FRONTEND_ORIGINS=https://www.example.com,https://staging.example.com

# Stripe secret key (server-side only, never exposed in client assets)
STRIPE_SECRET_KEY=sk_live_...

# Absolute HTTPS URLs for Stripe checkout redirects
CHECKOUT_SUCCESS_URL=https://www.example.com/checkout/success
CHECKOUT_CANCEL_URL=https://www.example.com/checkout/cancel
